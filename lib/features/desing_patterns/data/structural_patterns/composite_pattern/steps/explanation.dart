String explanation = """
### Explicación rápida:
* `MenuComponent` → interfaz común para menú y opciones.
* `MenuItem` → hoja (elemento indivisible).
* `MenuGroup` → compuesto (contiene otros MenuComponent).
* El cliente (`MyApp`) puede construir un árbol jerárquico de menús y renderizarlos sin distinguir entre grupo o ítem.
""";