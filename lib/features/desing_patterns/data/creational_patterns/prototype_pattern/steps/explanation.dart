String explanation = """
### Explicación rápida:
* `Prototype<T>` → interfaz con el método clone().
* `Usuario` → clase concreta que implementa clone() y además tiene copyWith() para clonar con variaciones.
* En la `UI` mostramos tres usuarios:
  * El original (usuario1).
  * Uno clonado idéntico (usuario2).
  * Uno clonado con cambios (usuario3).
""";