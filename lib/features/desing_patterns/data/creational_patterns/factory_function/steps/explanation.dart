String explanation = """
### Explicación rápida:
* `Shape` → interfaz común.
* `Circle` y `Square` → implementaciones concretas.
* `ShapeFactory(String type)` → factory function que devuelve el objeto adecuado según un parámetro.
* En `main`, pedimos "circle" o "square" y obtenemos instancias correctas sin exponer el new.
""";