String explanation = """
### Explicación rápida:
* `MediadorChat` → interfaz del mediador, define cómo se envían los mensajes.
* `ChatSala` → el mediador centraliza toda la comunicación, evitando que los usuarios se conozcan entre sí.
* `UsuarioChat` → colegas que solo se comunican a través del mediador.
* `UI (Flutter)` → simula un usuario enviando mensajes a otros a través del mediador.
""";